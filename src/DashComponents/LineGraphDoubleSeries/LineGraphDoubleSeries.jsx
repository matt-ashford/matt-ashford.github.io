import { useEffect, useState } from "react";

export const LineGraphDoubleSeries = (props) => {
  const { keeperProds, joinedDataQtr } = props;
  return (
    <>
      line grpah go burrr
      {/* <div>{JSON.stringify(joinedDataQtr)}</div> */}
    </>
  );
};

export default LineGraphDoubleSeries;
